import { CustomProperty } from "../../CustomProperty";

export interface CommonBackgrounds {
  allies: number;
  contacts: number;
  domain: number;
  generation: number;
  herd: number;
  influence: number;
  mentor: number;
  resources: number;
  retainers: number;
  status: number;
  customBackgrounds: CustomProperty[];
}
